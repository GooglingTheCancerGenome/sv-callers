#
# This workflow executes SV callers on tumor-normal sample pairs.
#

import pandas as pd

shell.executable("/bin/bash")
configfile: "config.yaml"

def get_samples():
    return pd.read_csv(config['samples'], sep='\t').itertuples()

rule all:
    input:
        [ "{0}-{1}.log".format(row.tumor, row.normal) for row in get_samples() ]

# include: "rules/samtools.smk"
include: "rules/manta.smk"
# include: "rules/delly.smk"
# include: "rules/lumpy.smk"
# include: "rules/gridss.smk"
